# src/adapter/app_adp/crop_adp.rs

## `pub fn crop_region_adp( window_title: &str, x: u32, y: u32, w: u32, h: u32, ) -> Result<String, AppError>`

*Line 8 · fn*

Captures `window_title`, crops `(x, y, w, h)`, returns the region as base64 PNG.

---

