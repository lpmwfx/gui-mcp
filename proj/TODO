# TODO: scaffold

phase: 1
id: scaffold

## Project Setup

- id: 1
  task: Write Cargo.toml with all dependencies (rmcp, tokio, image, imageproc, enigo, base64, serde, windows, rustscanners, rustdocumenter)
  pass: cargo build compiles without errors
  status: done

- id: 2
  task: Create build.rs with rustdocumenter and rustscanners
  pass: cargo build runs scanners without violations
  status: done

- id: 3
  task: Create src/state/mod.rs, sizes.rs, limits.rs with constants (confidence threshold, buffer size, rgb channels)
  pass: src/state module compiles and constants are accessible
  status: done

- id: 4
  task: Create .gitignore (/target)
  pass: git status does not show target/ directory
  status: done

## Topology Scaffold (hexagonal MVVM)

- id: 5
  task: Create 6-layer topology — src/ui, adapter, core, pal, shared, state
  pass: all layers compile, topology DAG enforced (ui→adapter→core/pal)
  status: done

- id: 6
  task: Implement src/ui/server_ui.rs — SlintGuiServer stub with ServerHandler + all 7 tool stubs
  pass: cargo build passes; rmcp tool list compiles
  status: done

## PAL Layer (platform abstraction)

- id: 7
  task: Implement src/pal/window_pal.rs — find_window_by_partial_title, capture_window, get_window_rect using Win32
  pass: Can capture screenshot of a real window and get an RgbImage back
  status: done

- id: 8
  task: Implement src/pal/input_pal.rs — click_at, type_text, send_keys using enigo
  pass: Clicking at known coordinates moves the mouse to the correct position
  status: pending

## Core Layer (business logic)

- id: 9
  task: Implement src/core/vision_core.rs — find_template NCC algorithm (stubs replaced)
  pass: find_template returns correct MatchResult on a known test image pair
  status: pending

## Adapter + UI dispatch

- id: 10
  task: Wire src/adapter/app_adp.rs dispatch functions to real PAL/Core implementations
  pass: screenshot_window returns a valid base64 PNG for a real window
  status: pending

- id: 11
  task: Wire src/ui/server_ui.rs tool stubs to app_adp calls; replace todo!() with real dispatch
  pass: MCP server responds correctly to screenshot_window and list_windows tool calls
  status: pending

## Blockers

- rmcp API verified: #[tool(tool_box)] + impl ServerHandler compiles with rmcp 0.1.5
